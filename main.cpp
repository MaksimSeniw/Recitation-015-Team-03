#include <iostream>
#include <vector>
std::vector<bool> LessMask(std::vector<double> nums, double less_than);
std::vector<bool> GreaterMask(std::vector<int> nums, double greater_than);
#redo
int main()
{
    return 1;
}
#what is going wrong